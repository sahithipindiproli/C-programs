# include<stdio.h>
# include<string.h>
main(){
	char s1[20],s2[20];
	int result;
	printf("Enter string 1");
	gets(s1);
	printf("Enter stribg 2");
	gets(s2);
	result==strcmp(s1,s2);
	if(result=0)
	printf("Strings are equal");
	else
	printf("strings are not equal");
}
